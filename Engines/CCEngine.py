from Engine import Engine
import time

class CC(Engine):
    #A CC engine take a speed in m/s, an angle in degree, a time in second and a wheel diameter in meter.
    def __init__(self):
        self.speed = 0
        self.angle = 0
        self.time = 0
        self.dWheel = 0.065

def setSpeed(self, speed):
    self.speed = speed

def setAngle(self):
    while self.running == True:
        self.angle += 1
